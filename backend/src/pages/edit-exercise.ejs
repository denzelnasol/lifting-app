<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>
          <% if(editing){ %>Edit Exercise<% } else { %>Add Exercise<% } %>
        </title>
    </head>
    <body>
        <h1><% if(editing){ %>Edit Exercise<% } else { %>Add Exercise<% } %></h1>

        <form method="POST" action="/<% if(editing){ %>edit-exercise<% } else { %>add-exercise<% } %>">
            <div>
                <label>Name</label>
                <input type="text" name="name" value="<% if(editing){ %><%= exercise.name %><% } %>" required />
            </div>
            <div>
                <label>Image</label>
                <input type="text" name="image" value="<% if(editing){ %><%= exercise.image %><% } %>" required />
            </div>
            <div>
                <label>Description</label>
                <input type="text" name="description" value="<% if(editing){ %><%= exercise.description %><% } %>" required />
            </div>
            <% if(editing){ %>
              <div>
                <input type="hidden" name="exerciseId" value="<%= exercise.id %>"/>
              </div>
            <% } %>
            <div>
                <button type="submit"><%if(editing) { %>Edit Exercise <%} else { %>Add Exercise<% } %></button>
            </div>
        </form>
    </body>
</html>
